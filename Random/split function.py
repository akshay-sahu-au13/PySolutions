Str = input("Enter a string:") #Enter a string with multiple spaces

Str1 = Str.split()  # Split will by default seperate the string elements
                    # by space and make it into a list

print(Str)
print(Str1)

Str2 = Str.split(",") # this will seperate the string after detecting comma.

print(Str2)

Str3 = Str.split("a")  ##  this will seperate the string after detecting "a".

print(Str3)

            
